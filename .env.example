# Local SQLite file:
# DATABASE_URL="file:./prisma/dev.db"

# Turso (production) — URL from Turso dashboard + token from: turso db tokens create shoe-mafia
DATABASE_URL="libsql://shoe-mafia-harshitshuklacc-max.aws-ap-south-1.turso.io"
TURSO_AUTH_TOKEN="paste-your-turso-token-here"

# Site URL (sitemap, emails, checkout links)
NEXT_PUBLIC_SITE_URL="https://your-domain.vercel.app"

# Auth (generate with: openssl rand -base64 32)
JWT_SECRET="change-me"

# Optional: real-time admin inventory/orders (separate Node server; not on Vercel serverless)
NEXT_PUBLIC_SOCKET_URL="http://localhost:3001"
